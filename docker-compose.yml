# Static UI only. Configure the separately deployed Cloudflare relay in the browser.
services:
  ohmyssh:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
